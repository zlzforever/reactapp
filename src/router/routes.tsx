import Empty from "../components/Empty";
import { RouteObject } from "react-router-dom";
const routes: RouteObject[] = [
  {
    path: "index",
    element: <Empty name="index"></Empty>,
  },
];

export default routes;
